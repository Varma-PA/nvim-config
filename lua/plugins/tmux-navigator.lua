return {
  "christoomey/vim-tmux-navigator",
  -- Seamless navigation between Neovim splits and tmux panes (Ctrl-h/j/k/l).
  -- When Neovim is inside tmux, same keys move between both.
  -- No config needed; ensure tmux is configured to use the same keys (see plugin README).
  lazy = false,
}
